{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path' | { read -r f; case \"$f\" in *.cs) cd \"$CLAUDE_PROJECT_DIR\" && dotnet format whitespace MediaTryk.sln --include \"${f#\"$CLAUDE_PROJECT_DIR\"/}\" --no-restore;; esac; } 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Formatting C# file..."
          }
        ]
      }
    ]
  }
}